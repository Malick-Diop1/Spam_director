{% extends 'base.html.twig' %}

{% block title %}Assureur{% endblock %}

{% block body %}
    <h1>Assureur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ assureur.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ assureur.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_assureur_index') }}">back to list</a>

    <a href="{{ path('app_assureur_edit', {'id': assureur.id}) }}">edit</a>

    {{ include('assureur/_delete_form.html.twig') }}
{% endblock %}
