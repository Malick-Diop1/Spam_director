<form method="post" action="{{ path('app_mode_reglement_delete', {'id': mode_reglement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mode_reglement.id) }}">
    <button class="btn">Delete</button>
</form>
